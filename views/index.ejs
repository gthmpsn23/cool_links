<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class ="container my-5">
    <h1 class="text-center mb-3"> URL Shrinker</h1>
    <div></div>
    <h2 class="text-center mb-4"> Turn your ugly URLs into short, custom links in a few clicks!</h2>
    <div></div>
    <ol class="mb-4">
    <li>Enter your current URL.</li>
       <li>Create your own custom link.</li> 
       <li>Click the "Create your URL".</li>
         </ol>
         <form action="/shortUrls" method="POST" class="my-4 row gx-3 gy-2 align-items-center">
            <label for="fullUrl" class="visually-hidden">Url</label>
            <div class="col-auto">
                <input required placeholder="Enter your current URL" type="url" name="fullUrl" id="fullUrl" class="form-control">
            </div>
            <div class="col-auto">
                <input placeholder="Enter your custom URL" type="text" name="customAlias" id="customAlias" class="form-control">
            </div>
            <div class="col-auto">
                <button class="btn btn-success" type="submit">Create your URL</button>
            </div>
        </form>
        
</div>

    <% if (errorMessage) { %>
        <div class="alert alert-danger mt-2">
            <%= errorMessage %>
            </div>
            <% } %>
        
    
<div class="mx-5">
    <table class="table table-striped table-responsive my-6">
        <thead>
            <tr>
                <th>Full URL</th>
                <th>Short URL</th>
                <th>Clicks</th>
            </tr>
        </thead>
        <tbody>
            <% shortUrls.forEach(shortUrl => {
                %>
                <tr>
                <td><a href="<%= shortUrl.full %>" target="_blank"><%= shortUrl.full %></a></td>
                <td><a href="<%= shortUrl.short %>" target="_blank"><%= shortUrl.short %>
                </a></td>
                <td><%= shortUrl.clicks %></td>
            </tr>
                <%
            }) %>
           
        </tbody>
    </table>
</div>
</div>
</body>
</html>